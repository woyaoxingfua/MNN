name: iOS Build (Unsigned IPA)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and Create Unsigned IPA
    runs-on: macos-latest

    steps:
      # 第 1 步：检出您的代码仓库
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # 第 2 步：选择一个稳定的 Xcode 版本
      - name: Select Xcode Version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.4'

      # 第 3 步：安装 CocoaPods 依赖
      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: apps/iOS/MNNLLMChat/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-
            
      - name: Install Pods
        run: |
          cd apps/iOS/MNNLLMChat
          pod install

      # 第 4 步：编译和归档 (Archive)
      - name: Archive Project
        run: |
          xcodebuild archive \
            -workspace apps/iOS/MNNLLMChat/MNNLLMChat.xcworkspace \
            -scheme MNNLLMiOS \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/MNNLLMChat.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      # 第 5 步：【已修正】手动创建未签名的 IPA
      - name: Manually Create Unsigned IPA
        run: |
          mkdir -p build/ipa/Payload
          # 修正点：将 MNLLMiOS.app 改为 MNNLLMChat.app
          cp -R build/MNNLLMChat.xcarchive/Products/Applications/MNNLLMChat.app build/ipa/Payload/
          cd build/ipa
          zip -r MNNLLMChat-unsigned.ipa Payload

      # 第 6 步：上传构建产物 (IPA)
      - name: Upload Unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: MNNLLMChat-unsigned-ipa
          path: build/ipa/*.ipa
